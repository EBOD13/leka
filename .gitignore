# Interview prep / personal notes -- never publish
INTERVIEW_PREP.md
*.private.md

# Build output
build/
build-*/

# Market data: raw ITCH sessions are hundreds of MB and are not ours to redistribute.
data/*
# ...but the small interchange samples the published viewer is generated from
# are tracked, so CI can rebuild the replay without fetching anything.
!data/interchange/

# Generated documentation
html/
latex/
doc/html/

# macOS
.DS_Store

# Recorded snapshots: regenerate with snapshot_recorder
docs/viz/data/
